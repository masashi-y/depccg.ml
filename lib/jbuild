(jbuild_version 1)


(library (
    (name depccg)
    (preprocess (pps (ppx_monadic ppx_sexp_conv)))
    (libraries (str ocaml_base ocaml-protoc ppx_monadic unix logic containers sexplib yaml yaml.unix xml-light shexp.process))
))

