(jbuild_version 1)


(executable (
    (name main)
    (public_name camelthorn)
    (preprocess (pps (ppx_deriving_argparse)))
    (libraries (str psq ocaml-protoc ppx_monadic unix logic thorn parmap))
))

